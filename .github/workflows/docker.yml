name: 🐋 Build and Push Docker Image

on:
    push:
        branches:
            - main
        paths:
            - "packages/*/src/**"
            - "apps/*/src/**"

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    build_and_push:
        runs-on: ubuntu-latest

        steps:
            - name: 📥 Checkout repository
              uses: actions/checkout@v6

            - name: #️⃣ Get short SHA
              id: sha
              run: echo "short_sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

            - name: 🔽 Set up Docker Buildx
              uses: docker/setup-buildx-action@v3

            - name: ✅ Log in to DockerHub
              uses: docker/login-action@v3
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_TOKEN }}

            - name: 🔨 Build image
              uses: docker/build-push-action@v6
              with:
                  context: .
                  load: true
                  tags: ${{ secrets.DOCKERHUB_USERNAME }}/msdbot_telegram:fat
                  cache-from: type=gha
                  cache-to: type=gha,mode=max
                  build-args: |
                      GIT_COMMIT=${{ steps.sha.outputs.short_sha }}

            - name: 🗜️ Install SlimToolkit
              run: |
                  curl -L -o ds.tar.gz https://github.com/slimtoolkit/slim/releases/download/1.40.11/dist_linux.tar.gz
                  tar -xvf ds.tar.gz
                  sudo mv dist_linux/docker-slim /usr/local/bin/slim
                  sudo mv dist_linux/docker-slim-sensor /usr/local/bin/slim-sensor

            - name: 🪄 Minify image
              run: slim build --http-probe=false --target ${{ secrets.DOCKERHUB_USERNAME }}/msdbot_telegram:fat --tag ${{ secrets.DOCKERHUB_USERNAME }}/msdbot_telegram:latest

            - name: 🚀 Push image
              run: docker push ${{ secrets.DOCKERHUB_USERNAME }}/msdbot_telegram:latest
